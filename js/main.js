/**
 * main.js - Inicializador do Sistema SCTEC
 */
document.addEventListener("DOMContentLoaded", () => {
    // Inicializa os controladores
    UIController.init();
    FormController.init();
    
    console.log("SCTEC System Ready - Desenvolvido por PH");
});